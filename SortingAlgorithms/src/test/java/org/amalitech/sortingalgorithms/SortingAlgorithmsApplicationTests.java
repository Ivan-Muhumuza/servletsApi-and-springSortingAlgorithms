package org.amalitech.sortingalgorithms;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Integration test to check if the Spring application context loads successfully.
 */
@SpringBootTest
class SortingAlgorithmsApplicationTests {

	@Test
	void contextLoads() {
		// This test method is empty because its purpose is to verify if the Spring context loads without errors.
		// The presence of this method annotated with @Test ensures that Spring Boot will attempt to load the context.
		// If the application context fails to load (e.g., due to configuration errors), this test will fail.
	}
}
